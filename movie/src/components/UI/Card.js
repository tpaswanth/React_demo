import styled from "styled-components";

const Card = styled.div`
padding: 2rem;
box-shadow: 0 2px 8px rgba(0, 0, 0, 0.25);
border-radius: 14px;
background-color: rgba(250, 245, 245, 0.25); 
width: 25%;
   `;

export default Card;
